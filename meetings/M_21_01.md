# What I did:
- Wrote a test setup to make testing elliptic curve algebra in circom easier.
- Fixed the scalar multiplication for montgomery curves.
- Changed the montgomery ladder so that the multiplier can have at most as many Bits as the parameter says, but does not need to have exactly as many Bits.
    - This was actually wuite complicated and required quite a lot of additional constraints. 
    - As a result, my montgomery ladder algorithm for 256 Bits now needs 9269 constraints not the roughly 5000 I had before. (Now, 1205 constraints for 32 bits)
    - Is there maybe an easier way to do this?
- 